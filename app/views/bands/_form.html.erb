<%= simple_form_for @band, :defaults => {:required => false}, :html => {:class => "form"} do |f| %>
<%= f.input :name %>
<%= f.input :image %>
 <div class="actions">
  <%= f.submit %>
</div>
<% end %>

